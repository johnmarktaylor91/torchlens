"""Unit testing script. The unit tests include:
- Does the package leave the network unchanged, and does it leave the torch namespace unchanged?
- Does it correctly reconstruct the inputs for a representative sampling of different networks?
"""
