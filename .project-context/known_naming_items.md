# Known Naming Items

## Intervention API

- `confirm_mutation` and `suppress_mutate_warnings` use different verbs for
  the same mutate-warning concept. Defer any rename to JMT's dedicated naming
  pass; do not rename during Phase 5c maintenance.
