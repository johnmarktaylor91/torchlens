# combined-sprint BUILD loop state
run_name: combined-sprint-build
state: RUNNING (foundation phases)
plan: .research/combined-sprint/combined-sprint_PLAN.md (v6; early phases build-ready)
branch: capture-unification; accumulate-on-branch, NO push until JMT
## File-family locks (serialize within family): F-DATAMODEL / F-IO / F-POSTPROC / F-JAX / F-TINYGRAD /
## F-REGISTRY-API / F-VALIDATION / F-TESTS-DOCS-GOLDENS. See plan §1.
## Per-round gate: ruff+mypy touched; predicate suite (test_capture_unification_p3a..p7) + smoke + parity
## goldens green. Phase boundary: full not-slow + relevant .tlspec digest. d589c1c5 bisect protocol.
## CHECKPOINT 1 after JAX-INTERP+EQX-PROBE: re-estimate JAX lane, confirm w/ JMT before B2/B7/B8/B9.
## CHECKPOINT 2 after B-CODEC. B-REFACTOR separate PR by default.
## Foundation build order (cleared): A0 -> A1 -> STATE-ADAPTER -> A2 -> A-Q(parallel) -> V0 -> API-CONTRACT
##   -> JAX-INTERP(sub1 spine -> sub2 control-edge -> sub3 grouping-adapter -> sub4 equiv-key -> sub5 unified)
##   -> EQX-PROBE -> CHECKPOINT1.
## Autonomous decision log: see SYNTHESIS_NOTES.md + plan ledger. JAX T1 = internal test-only.
## tinygrad/JAX venvs at /tmp/jaxtg/{jax,tinygrad}-venv. Canonical jax/tinygrad test cmds in /tmp/jaxtg/*.
## Iteration log
| Round | Phase | Commit | Result | Next |
|---|---|---|---|---|
| 1 | A0 instrumentation/baseline | 8e62a73b | Added `benchmarks/op_slots_baseline.py`. Baseline (`/home/jtaylor/anaconda3/envs/py311/bin/python benchmarks/op_slots_baseline.py`): parity_mlp 5 ops, 33,160 total Op+dict bytes, 6,632.0 bytes/op, object_construction:op 0.427 ms total / 106.628 us/op; small_resnetish 38 ops, 252,016 bytes, 6,632.0 bytes/op, 3.741 ms / 91.239 us/op; synthetic_100,000_ops 100,000 ops, 663,200,000 bytes, 6,632.0 bytes/op, 9,599.093 ms / 95.991 us/op. Note: an initial full-capture 100k chained torch-op attempt was stopped after ~28 min at ~68 GB RSS; the committed synthetic row constructs 100k `Op` instances from a real traced seed op and attaches them to a trace object to isolate the slotting target. | A1 reorder no-op gate. |
| 1 | A1 reorder no-op | 183170f2 | Made `_trim_and_reorder_layer_entry_fields` an intentional no-op and replaced Op rename writes in `_replace_layer_names_for_layer_entry` with `_internal_set`/attribute reads. FIELD_ORDER unchanged; derived input fields retained. Gates passed: `ruff check . --fix`; `mypy torchlens/postprocess/labeling.py benchmarks/op_slots_baseline.py`; predicate suite 31 passed; smoke 250 passed, 5 skipped; backend parity 12 passed. `tests/backend_parity/goldens` had no diff; goldens were byte-identical and not regenerated. | STATE-ADAPTER. |
| 2 | STATE-ADAPTER | STOP-no-commit | Added scoped adapter worktree changes for `torchlens/data_classes/_state_adapter.py` and rerouted scrub, rehydrate, bundle blob walkers, `Op` pickle, Trace fork/rerun copies, plus direct adapter tests. Gates passed before stop: `ruff check . --fix`; `mypy torchlens/`; predicate suite 36 passed; smoke 250 passed, 5 skipped; backend parity 12 passed; `tests/backend_parity/goldens` had no diff; adapter import check clean. STOP reason: raw `.tlspec` save->load->resave byte digest still differs on two fixtures in `metadata.pkl` only. Unpickled metadata is semantically equal and top-level/nested dict order checks found no remaining value/order diff after normalization, so remaining mismatch appears to be pickle memo/alias byte identity after rehydrate. Per plan gate, no commit made. | Decide whether STATE-ADAPTER gate should require raw pickle byte identity or a canonical semantic metadata digest; then finish/commit or revise rehydrate alias preservation. |

## DEFERRED F-IO ITEM (found via perf baseline, 2026-06-13)
The committed-baseline benchmark (perf_suite at 67fdf296 = v2.19.0 code) FAILED again — NOT a torchlens
crash in normal use, but a `name 'd' is not defined` NameError in the aux_save/aux_load/aux_validate cells
when SAVING/LOADING/VALIDATING a FULL default resnet18 trace. NOT in scrub.py (checked). Likely
rehydrate.py / bundle.py / validation save path; a code path the not-slow .tlspec tests don't cover
(they pass). => v2.19.0 has a latent serialization NameError on full-trace save/load surfaced only by the
benchmark. ACTION (do AFTER STATE-ADAPTER lands, on settled F-IO files; same family): root-cause the `d`
NameError (reproduce aux_save of a full trace for the exact file:line), fix it, ADD a full-model
save/load/validate regression test (the coverage gap). THEN re-run the committed baseline (non-gating,
deferred). Do NOT parallel-edit F-IO while state-adapter codex (pid 3013636) is in those files.
| 1 | A0+A1 | 8e62a73b,183170f2 | A0 baseline (6632 B/op); A1 reorder no-op, parity BYTE-IDENTICAL (de-risks slots). | STATE-ADAPTER |
| 2 | STATE-ADAPTER | ad89b51a | generic _state_adapter.py; scrub/rehydrate/bundle/pickle routed through it; semantic-equal verified (manifest+body SHA identical, op-projection identical; only metadata.pkl memo bytes differ = M-12-expected, ACCEPTED). adapter completeness test green. GATE CORRECTION: .tlspec gate is SEMANTIC-digest+manifest/body-SHA, NOT raw pickle bytes. | A2 slots (fold in the d-NameError fix via full-model fixture) |
| 3 | A2 Op slots | c5de1ea8 | Added slot-backed `Op` with derived/property-backed fields excluded and slot-safe state access through `_state_adapter`; fixed cleanup, replay/backward grad clearing, fork facet invalidation, streaming finalization, tests/benchmarks, and full-model bundle validation. `d` NameError was NOT hit; full ResNet18 save/load/schema validation instead exposed missing `buffer_initial_value` manifest vocabulary and manifest `created_at` churn, both fixed with regression coverage. Gates green: ruff, mypy, smoke, backend parity byte-identical, .tlspec semantic+manifest/body SHA identical on 3 fixtures including full ResNet18, not-slow. Benchmark: 1456 B/op vs 6632 B/op baseline (78.0% reduction). | A-Q perf quick wins / next foundation phase. |
| 3 | A2 slots | c5de1ea8 | __slots__ Op: MEMORY 6632->1456 B/op (~78%!); parity byte-identical; .tlspec semantic+SHA identical on MLP/selective/RESNET18-FULL; not-slow 2711 passed. d-NameError NOT reproduced (state-adapter reroute resolved it); full-resnet18 gate caught+fixed 2 OTHER serialization bugs (buffer_initial_value manifest vocab, created_at re-save churn) w/ regression tests. | A-Q + V0 + API-CONTRACT (parallel, disjoint families) |
| 4 | V0+API-CONTRACT+A-Q (parallel) | 8f72cb5a,afc015d4,9b831315 | V0 per-mode module invariants + function_root minimal + corruption fixtures (MLX stays torch-path); API-CONTRACT public-option spine + capability epochs + 3-source agreement test; A-Q PERF-8 deque/PERF-38/39 (GC-10 deferred). All gate-green, parity byte-identical. FOUNDATION COMPLETE. | foundation phase-boundary not-slow gate -> JAX-INTERP |
| 5 | foundation phase-boundary gate | (n/a) | full not-slow 2723 passed / 0 failed. Foundation composes clean. | JAX-INTERP sub-phase 1 (replay-kind spine) |
| 6 | JAX-INTERP sub-phase 1 replay-kind spine | STOP-no-commit | Added scoped changes for `JaxEquationCapture.kind`, the 5-kind replay dispatch table, validation alignment keyed by `jax_capture_kind`, and direct dispatch-table tests. Gates green: `ruff check . --fix`; `mypy torchlens/backends/jax/backend.py torchlens/backends/jax/jaxpr.py torchlens/ir/events.py tests/test_jax_backend_validation.py`; `mypy torchlens/`; predicate suite 31 passed; smoke 250 passed / 5 skipped; backend parity 12 passed with no golden diff. STOP reason: canonical JAX command is red outside F-JAX scope: 46 passed / 1 failed, `tests/test_jax_backend_hardening.py::test_jax_public_surface_matrix` raises `TorchLensIOError: Op.out expected a tensor for portable blobification, got ArrayImpl` in `_io/scrub.py` before `_io/bundle.py` can raise the expected JAX audit-only `BackendPayloadUnsupportedError`. Per scope lock, F-IO was not changed and no commit was made. | Decide whether to fix the F-IO audit-only rejection ordering now or accept a known red canonical JAX gate; then re-run JAX gate and commit sub-phase 1 before B-CF control-edge contract. |
| 6 | JAX-INTERP sub-phase 1 | 36fcd305 | kind-dispatched replay spine (primitive + 4 stub kinds); behavior-neutral; jax primitive replay routed through dispatch table. | FIX F-IO audit-only regression (state-adapter broke it) THEN sub-phase 2 |
## REGRESSION FOUND (state-adapter/A2 F-IO work, missed because foundation gates ran BASE not-slow where
## jax/tinygrad tests SKIP): non-torch audit-only PORTABLE-SAVE broke. jax test_jax_public_surface_matrix +
## tinygrad test_tinygrad_public_surface_matrix fail: scrub now blobifies the non-torch array (ArrayImpl/
## Tensor) and raises "expected a tensor", instead of the clean BackendPayloadUnsupportedError. LESSON:
## ALL F-IO phases (incl B-CODEC) MUST gate on the jax+tinygrad venv suites, not just base not-slow.
| 7 | F-IO audit-only fix | b8c2e32e | restored non-torch audit-only portable-save guard; jax venv 47, tinygrad venv 36, MLX audit-only, parity 12, smoke 250 ALL GREEN. Backends restored. NEW GATE DISCIPLINE: F-IO phases run venv suites. | JAX-INTERP sub-phase 2 (control-edge contract) |
| 8 | JAX-INTERP sub-phase 2 control-edge contract | this commit | Added `EdgeUseKind` control, unconditional `_edge_uses` persistence with tuple emission normalized to `EdgeUseRecord`, JAX data/control parent classification helpers, replay/perturb exclusion for control parents, generic saved-validation data-parent filtering, and synthetic control-edge tests for pruning retention plus JAX replay/perturb exclusion. Gates green: `ruff check . --fix`; `mypy torchlens/`; predicate suite 31 passed; smoke 250 passed / 5 skipped; backend parity 12 passed and `tests/backend_parity/goldens` byte-identical; JAX venv 49 passed; tinygrad venv 36 passed; full not-slow 2726 passed / 27 skipped / 2 xfailed. | JAX-INTERP sub-phase 3 grouping adapter; B-CF can emit decision/control edges against this contract. |
| 8 | JAX-INTERP sub-phase 2 | 7865b1a6 | control-edge kind + classifiers; _edge_uses persists unconditionally (goldens byte-identical); control parents retained for topology, excluded from replay/perturbation; synthetic tests. jax venv 49, tinygrad 36, not-slow 2726, parity 12. | sub-phase 3 (loop-grouping graph adapter) |
| 9 | JAX-INTERP sub-phase 3 grouping adapter | not committed | Added backend-neutral `RecurrenceGroupingGraph`/`RecurrenceNode`/`RecurrenceAssignment` and `group_recurrent_nodes`; torch Step-7 now builds the neutral graph and applies returned assignments. Added neutral-service recurrent fixture test. Gates green: `ruff check . --fix`; `mypy torchlens/`; backend parity 12 passed with goldens byte-identical; JAX venv 49 passed; tinygrad venv 36 passed; full not-slow 2727 passed / 27 skipped / 231 deselected / 2 xfailed. No JAX wiring yet. | sub-phase 4 (JAX equivalence key + build neutral graph/wiring) |
| 9 | JAX-INTERP sub-phase 3 | be2ed0f6 | backend-neutral loop_grouping_adapter.py; torch Step-7 consumes it BYTE-IDENTICAL (recurrent labels/goldens unchanged); not-slow 2727. shared grouping service ready for JAX. | sub-phase 4 (JAX equivalence key + wire JAX finish into adapter) |
| 10 | JAX-INTERP sub-phase 4 | this commit | JAX equation equivalence key is primitive + input/output avals + normalized source path (leaf equation ordinal and scan/while iteration markers stripped; control/call-site path retained). JAX `_finish_trace` now builds `RecurrenceGroupingGraph` with DATA edges and calls `group_recurrent_nodes`; no force-stamping `pass_index=1`. Added repeated add/mul block test proving multi-pass JAX grouping while replay/perturbation and metadata validation stay green. Gates green: `ruff check . --fix`; `mypy torchlens/`; JAX venv 50 passed; tinygrad venv 36 passed; predicate 31 passed; smoke 250 passed / 5 skipped; backend parity 12 passed with goldens byte-identical. | sub-phase 5 attribution/EQX-PROBE before CHECKPOINT 1; B-CF can depend on JAX grouping foundation. |
| 12 | B-CF round 1 scan unroll | this commit | Implemented `lax.scan` unroll in the JAX interpreter before the nested-primitive reject gate: emits graph-visible `scan_read` per scanned leaf/iteration, interprets body jaxprs with carry threading, emits `scan_stack` for ys outputs, and implements deterministic replay for both synthetic kinds. JAX `jax_control_flow` now defaults to `unroll`; `reject` preserves old scan rejection; `jax_max_control_flow_unroll` guards scan length. Body ops group into multi-pass layers via the JAX equivalence key/shared grouping adapter; tests cover scan replay+perturb validation, 4-iter grouping, two separate scans no-overgroup, and same primitive outside scan no-overgroup. Gates green: `ruff check . --fix`; `mypy torchlens/`; JAX venv 52 passed; tinygrad venv 36 passed with `DEV=PYTHON`; predicate 31 passed; smoke 250 passed / 5 skipped; backend parity 12 passed with goldens byte-identical. | B-CF round 2: cond/while unroll with decision/control helper captures and guards. |
| 13 | B-CF round 2 cond/while unroll | this commit | Implemented `lax.cond` executed-branch unroll with `cond_decision` control helper replay validation, implemented `lax.while_loop` dynamic unroll with cond/body frames, `while_decision` stop-point replay validation, zero-iteration carry passthrough, iteration grouping via existing JAX equivalence keys, and max-unroll guard. Decision nodes are retained as topology/control dependencies and excluded from perturbation while still replay-validating the recorded branch/stop. Gates green: `ruff check . --fix`; `mypy torchlens/`; JAX venv 57 passed; tinygrad venv 36 passed with `DEV=PYTHON`; predicate 24 passed; smoke 250 passed / 5 skipped; backend parity 12 passed with goldens byte-identical. | B-CF COMPLETE -> sub-phase-5-attribution + B2 Equinox modules via named_scope. |
| 10 | JAX-INTERP sub-phase 4 | 16a73854 | JAX equivalence key (primitive+avals+normalized-source) + JAX finish routes through shared grouping service (no more pass_index=1 force); repeated ops group multi-pass torch-consistently; replay per-op green. jax venv 50, tinygrad 36, parity byte-identical. 1 jax test expectation updated (pass-qualified labels, justified). B-CF grouping foundation READY. | EQX-PROBE -> sub-phase 5 -> CHECKPOINT 1 |
| 11 | EQX-PROBE | (spike) | equation-index-range attribution REFUTED (live boundaries don't match final jaxpr eqn indices; jit caching breaks one-pass assumption). jax.named_scope REGION METADATA validated: wrap module __call__ in named_scope(module_addr) during derivation -> final eqns carry name_stack -> attribute each Op to owner. Works a/b/c (simple/nested/shared); jit-inside cold-only (reject init); cond/scan-inside -> B2a strict rejects. equinox==0.13.8 pin. | CHECKPOINT 1 |

## >>> CHECKPOINT 1 (autonomous decision, logged for JMT review) <<<
JAX-INTERP foundation DONE (sub 1-4 committed 36fcd305/7865b1a6/be2ed0f6/16a73854; torch byte-parity held).
EQX-PROBE verdict: B2 attribution uses jax.named_scope REGION METADATA (NOT equation-index ranges — sub-phase
5 redesigned accordingly). No JMT fork (clear best answer). RE-ESTIMATE: JAX lane feasible; B-CF ready now
(needs only sub 1-4); B2 ready via named_scope (B2a strict: reject jit/cond/scan-inside-module initially).
DECISION (autonomous mandate): proceed to B-CF, then sub-phase-5-attribution + B2. iMessage sent to JMT.
| 12 | B-CF r1 scan | 0b5adddb | lax.scan UNROLL: per-iter scan_read/scan_stack synthetic replay captures; body ops GROUP into multi-pass layers (torch-consistent); two-scan no-overgroup verified; reverse handled; jax_control_flow + max-unroll guard. jax venv 52, tinygrad 36, parity byte-identical. | B-CF r2 (cond + while) |
| 13 | B-CF r2 cond+while | 91f93fc3 | lax.cond (executed-branch + cond_decision) + lax.while_loop (unroll + while_decision + max-guard + zero-iter); iterations group; rejected set kept. jax venv 57, tinygrad 36, parity byte-identical. B-CF COMPLETE (scan/cond/while all unroll). | B2 Equinox modules (named_scope attribution) |
| 14 | B2 round 1 Equinox pytree_module | this commit | Equinox `eqx.Module` roots default to `pytree_module`: class-level `__call__` wrappers filtered by instance id emit reversible `jax.named_scope` markers; captured equations decode `name_stack` module stacks; accepted nested library calls inherit parent module scopes. Dataclass module walk builds `_module_metadata`, `_module_build_data`, real `Trace.modules`, `training=False`, and pytree-derived params with exact op-param attribution from closed-jaxpr const object ids. B2a strict rejects `jit`/`cond`/`scan`/`while`/effects inside attributed modules with module-address errors. Gates green: `ruff check . --fix`; `mypy torchlens/`; JAX venv 63 passed; tinygrad venv 36 passed with `DEV=PYTHON`; predicate 31 passed; smoke 250 passed / 6 skipped; backend parity 12 passed with goldens byte-identical. | B2 round 2 shared modules + executable surface matrix + forward_args |
| 14 | B2 r1 Equinox modules | 4b9d3dba | pytree_module via named_scope: simple+nested modules, address tree, pytree-derived params, _build_module_logs delegation, V0 invariants pass, B2a strict (jit/cond/scan/while-inside rejected w/ module-addr error). modules.py 565 + test 254. jax venv 63 (7 module), tinygrad 36, not-slow 2726, parity byte-identical. | B2 r2 (shared modules + surface matrix + forward_args) |
| 15 | B2 r2 Equinox shared modules + surface matrix + forward_args | this commit | Shared Equinox module instances now use primary-address module identity with aliases in `all_addresses`, dense per-call ModuleCall keys, alias-aware pytree param ownership, and call-index named-scope metadata. Pytree-module captures retain public ModuleCall `forward_args` from the real forward pass. Added shared-module/forward_args tests plus executable pytree public-surface matrix (modules/selectors/children/params/draw/pandas/summary/audit save-load/validate/grads/log_backward). Gates green: ruff, mypy, jax venv 75, tinygrad 36, predicate 31, smoke 250/6 skipped, backend parity 12 with goldens byte-identical. | B2 COMPLETE -> B3 tinygrad modules (SPIKE-GATED) + B6 T1 |
| 15 | B2 r2 (B2 COMPLETE) | f6c83081 | Equinox shared modules (alias resolution, dense multi-call labels), executable surface matrix, ModuleCall.forward_args. jax venv 75, tinygrad 36, parity byte-identical. JAX MODULE HIERARCHY COMPLETE. | B3 tinygrad modules (spike-gated) |
| 16 | B3 tinygrad object_module | not committed | Spike GO: `_observe_tensor_ops` + scoped callable-object `__call__` interception attributes simple/nested/shared tinygrad module UOps correctly; duplicate/reused UOps retain first-construction attribution. Built auto/opt-in `object_module` for discovered tinygrad callable object graphs, with raw functions and explicit `function_root` unchanged. Gates green: ruff, mypy, tinygrad venv 45, jax venv 42, predicate 31, smoke 250/7 skipped, backend parity 12 byte-identical/no golden diff, backend registry 14. | B6 T1 |
| 16 | B3 tinygrad object_module | 3dd4770f | spike GO; object_module via _observe_tensor_ops + scoped __call__ interception (simple/nested/shared correct; dup-UOp=first-construction documented); function_root default unchanged; V0 invariants + surface matrix + glossary/docs. tinygrad venv 45, jax 42, parity byte-identical, registry 14. BOTH BACKENDS HAVE MODULE HIERARCHY. | B6 T1 (tinygrad shippable; JAX test-only) |
| 17 | B6 T1 intermediate derived gradients | not committed | tinygrad T1 ships behind `GradOptions(intermediate_grads=True)`: separate no-realize pass retains live Tensor API results, calls `loss.backward()` before realization, maps by conservative UOp signature + retained ordinal + parent signatures + shape/dtype, skips ambiguous duplicates, restores user grads, and exposes `trace.intermediate_derived_grads` plus read-only `op.derived_grad` without populating `op.grads`/`saved_grad_ops`. JAX adds only a private capped boundary-VJP oracle for tests, with no public trace surface/docs. Gates green: ruff, mypy, tinygrad venv 46, jax venv 49, predicate 31, smoke 250/7 skipped, backend parity 12 byte-identical/no golden diff. | B-CODEC (payload codec, F-IO, checkpoint 2 after) |
| 17 | B6 T1 intermediate grads | 0aaa44a8 | tinygrad T1 SHIPS: no-realize backward pass, conservative signature mapping (reject-ambiguous), trace.intermediate_derived_grads (property-backed, NOT in MODEL_LOG_FIELD_ORDER -> torch parity byte-identical) + op.derived_grad; op.grads still raises non-torch. JAX T1 = private capped boundary-VJP oracle, tests only, no public surface/docs. tinygrad venv 46, jax 49, predicate 31, smoke 250, parity byte-identical. | B-CODEC |
| 18 | B-CODEC round 1 schema/codec write path | this commit | Added backend-neutral payload codec registry plus torch/JAX/tinygrad codecs; generalized scrub `BlobSpec` to carry logical backend; bundle write path dispatches through codecs while torch keeps legacy byte-identical safetensors writes. Added optional v2 tensor/body-index codec vocabulary and `array_payloads` payload policy enum; `payload_kinds` wired from saved entries but remains empty until backend materialization capability flips. Gates green: ruff, mypy, TLSPEC/parity 36 passed with goldens unchanged, JAX venv 48, tinygrad venv 34, full not-slow 2727. Public JAX/tinygrad portable save remains audit-only. | B-CODEC round 2 load path/lazy refs; later fallback+capability flip |
