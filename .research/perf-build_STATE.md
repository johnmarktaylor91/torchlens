---
run: perf-build
created: 2026-06-12T09:04:28-04:00
state: RUNNING
current_round: 15
---

# perf-build -- Autonomous Loop State

This file is the canonical "where are we" record for this run.
Every wake-up event (watcher fire, user ping, schedule trigger) must
read this file FIRST and act on the case routing below given the
current observable state. Do not act on intuition; act on the case.

## Stop criteria (observable, quantitative)

all 6 phases of perf-sprint-PLAN.md committed on capture-unification (P0 harness+profiling, P1 engine, P2 ctx+zero-copy, P3 halt+rerun, P4 memory, P5 cleanups, P6 gate+numbers) AND pytest tests/ -m "not slow" exits 0 AND benchmark gate green

max_rounds: 30  # perf sprint is 6 phases + profile-first per-step verification; converges steadily. Anti-flail (3 rounds same issue) governs.

## Wake-up case routing

When a wake-up event fires, use this table BEFORE doing anything else:

| Observable signal | State | Action |
|---|---|---|
| codex pid alive | RUNNING | ack the user, yield turn (do not poll); next event will fire |
| codex exited cleanly + worktree advanced (HEAD changed) | ROUND_DONE | run post-round verification (tests / lint / project-specific) |
| post-round verification PASS, stop criterion not met | NEXT_ROUND | dispatch round N+1 with refreshed prompt |
| post-round verification PASS, stop criterion met | SHUTDOWN | run shutdown procedure below |
| post-round verification FAIL | RECOVER | focused fixup spec (failing test/benchmark + file:line); ONE retry per failure class then RESIDUAL. NOTE: a perf REGRESSION (gate fail) is a real failure, not just test-red. |
| codex hit quota (CODEX_FAILED with usage_limit reason) | QUOTA_BLOCKED | fall back per "Fallback chain" below |
| same un-closeable issue 3 rounds in a row | RESIDUAL | accept as residual, log it, continue or shut down |

Edit this table with run-specific cases. The defaults are minimums.

## Fallback chain (resource limits)

1. Primary: codex via /codex:rescue --background OR codex-bg.sh
2. Quota blocked: pivot to Agent(subagent_type="general-purpose", model="opus")
   with the spec adapted (drop XML scaffolding, keep contracts + file:line refs).
3. Both blocked: write `state: BLOCKED` here, send iMessage to JMT
   "blocked, will resume <reset-time>", schedule a wakeup, stop.

NEVER silently stall. NEVER export OPENAI_API_KEY to "work around" quota.

## Shutdown procedure (mechanical -- user is asleep)

When stop criterion triggers, run these in order:

1. Orchestrator personal inspection: profile-attributed before/after per phase (steady-state x toward 2-5x; halt rows measured; rerun 5-50x; dedup id-keyed correct; alias gate validation-green; param re-fetch). Fixup rounds for misses. PROFILE-FIRST: each win attributed to a phase-timing bucket.
2. Write SUMMARY.md (.research/perf-build_SUMMARY.md): per-phase before/after numbers, the headline table, what landed vs deferred, glossary names (halt=/save_mode/recurrent_layers), claim-honesty gate status (which README numbers are MEASURED vs aspirational).
3. iMessage JMT with the headline perf numbers + the honest halt story (measured, not aspirational).
4. Update todos.md (strike the ~20 fixed PERF/GC; mark sprint items done) + autonomous_gate.json.
4. Mark this file: `state: DONE`, append round N+1 = "shutdown" to log.
5. Send a final iMessage: "Run perf-build done. <one-line result>. <count> artifacts shipped."

No "figure out what to do at the end" -- spell it out concretely BEFORE
round 1 dispatches.

## Iteration log (append per round)

| Round | Start | End | Commit | Score / Result | Notes |
|---|---|---|---|---|---|

## Iteration log
| Round | Phase | Commit | Result | Remaining |
|---|---|---|---|---|
| 1 | P0 (partial) | 83623cd0 | Harness repaired + always-on profiling (verified buckets populate; PORTABLE_STATE_SPEC updated); smoke 241; no-save addendum CPU+CUDA pass. Surfaced+fixed a real bug (no-save invariant read unsaved op.out). | P0.3: rerun fresh CPU+CUDA baseline w/ fixed harness + commit per-SHA baseline JSON. THEN P1.1. Profile-first: each P1 step needs before/after phase-timing delta. |
| 2 | P0.3 BLOCKED | (none) | Full baseline blocked: host at load avg ~20 (JMT's definitive_fidelity_analysis.py x8). Codex correctly refused to commit a misleading baseline; restored touched files, tree clean. | JUDGMENT-CALL DEVIATION (orchestrator): committed quiet-host baseline DEFERRED to a quiet window before P6; proceed with P1 on profile-first RELATIVE deltas (load-robust). Do NOT touch the fidelity workers. |
| 3 | P1.1 | 86de2cd9 | Landed `_position_was_mutated` identity short-circuit. Profile delta on loaded host, TinyNet `tl.trace(profile=True)`: CPU dispatch median 1.1063s -> 0.4514s (-59.2%); CUDA dispatch median 0.03942s -> 0.03527s (-10.5%). Gates: ruff touched file pass; mypy touched file pass; focused perf+validation 77 pass; smoke 241 pass; benchmark smoke 26/26 cells ok. | Continue P1.2: gate `detect_torch_alias_contract` at both fastlog and exhaustive sites with `save_arg_values or intervention_active or backward_active or validation_active`; keep alias contract consumed-path tests green and measure dispatch bucket delta. |
| 3 | P1.2 | d09498ec | Gated full mutation-position alias detection behind the consumed-path predicate and kept default-path cheap output alias semantics (`unknown_aliasing=False`). Added `test_save_arg_values_keeps_inplace_alias_contract_versions`. Profile delta on loaded host from P1.1 baseline: CPU dispatch median 0.4514s -> 0.4432s (-1.8%); CUDA dispatch median 0.03527s -> 0.03423s (-2.9%). P1.1 correctly owns the shared tensor-compare win. Gates: ruff touched files pass; mypy backend files pass; focused perf+validation 79 pass; smoke 241 pass; benchmark smoke 26/26 cells ok. | Continue P1.3: identity dedup keyed on `id(source_tensor)` with per-pass strong refs to SOURCE, fix live save path and `Op.save_activation`, add dedup/pinning tests, measure clone+save and peak-memory deltas. |
| 3 | P1.1+P1.2 | 86de2cd9, d09498ec | Identity mutation short-circuit + alias-contract gate (the two big steady-state wins). 77/79 focused + 241 smoke + 26/26 bench-smoke green; validation intact (alias contract kept on where consumed, cheap output-alias preserved). | P1.3 next (id-dedup BOTH sites + source strong-ref pin + freed/realloc no-false-merge test). Benchmarking relaxed per JMT; quiet-host baseline still deferred. |
| 4 | P1.3 | f79d29e5 | Default saved-activation dedup is now same-object identity keyed on `id(source_tensor)` with the cache value pinning the source tensor; full content hashing remains internal opt-in via `_out_dedup_mode="content"`. Fixed both `_save_activation_fields` and `Op.save_activation`; pass-end cleanup clears source pins. Tests cover same-source dedup, equal-content different-source miss, collision/no-false-merge guard, replay/slow path, and cache release. Gates: ruff check . --fix pass; mypy touched source pass; tests/test_capture_targets.py 9 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is clone_save by removing per-saved-output CPU/GPU content hashing from default saved-output paths. | Continue P1.4: PERF-29 avoid redundant memory calls while preserving `pause_logging`; quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 4 | P1.3 | f79d29e5 | Identity dedup (source strong-ref pinned, both save sites, collision/replay/cache-release tests; full-content dedup private opt-in _out_dedup_mode). The 4-round-hardened correctness piece, shipped clean. smoke 241, capture-targets 9. | P1.4 (PERF-29 memory-call reduction, keep pause_logging) next, then P2 ctx+zero-copy. |
| 5 | P1.4 | c2387a03 | Added metadata-based dense tensor memory accounting and used it in forward activation save/projection/source/rerun paths while preserving the `pause_logging()` fallback for sparse/meta/unusual tensors. Added dense and sparse helper tests. Gates: ruff check . --fix pass; mypy touched source pass; tests/test_internals.py::TestGetTensorMemory 4 pass; tests/test_capture_targets.py 9 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is dispatch/object-construction/clone_save by avoiding redundant decorated `nelement()`/`element_size()` calls in activation metadata paths. | Continue P2.1: defer Op-level code-context/module-address work where safe; quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 5 | P1.4 (P1 COMPLETE) | c2387a03 | PERF-29 memory-call reuse (scoped to forward activation accounting; param/grad paths kept on get_memory_amount; pause_logging preserved). PHASE 1 DONE: all 4 steady-state engine wins in. test_internals 4, capture-targets 9, smoke 241. | P2.1 next (lazy ctx: defer Op code-context/module-address; keep recent_ops eager under followed_by). Then P2.2 save_mode, P2.3 arg leanness. |
| 6 | P2.1 (partial) | e1ecce20 | Added per-trace code-context identity cache for Op construction: repeated identical filtered call-stack identities reuse lightweight `FuncCallLocation` objects while preserving `save_code_context=False` identity fields, column-offset distinctions, and portable I/O by dropping `_code_context_cache`. Added cache regression test. Gates: ruff check . --fix pass; mypy touched source/tests pass; focused introspection/metadata/conditional/backward-streaming 7 pass/1 skip; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is object-construction/dispatch by avoiding repeated code-context location construction and bytecode column-offset work for identical hot call sites. | Continue P2.1: assess remaining module-address/context deferrals; if no safe scoped work remains, proceed to P2.2 save_mode enum. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 7 | P2.1 (complete) | 34332fac | Reused the same per-trace code-context cache for exhaustive module-entry metadata, so repeated module calls at the same call site share `FuncCallLocation` objects instead of rebuilding stack location metadata. Added finalized-trace module-call regression coverage. Gates: ruff touched files pass; mypy touched source/tests pass; tests/test_introspection.py 4 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is object-construction/dispatch for module-heavy traces with repeated module call sites. | Continue P2.2: `save_mode` enum (copy/reference/view/cpu_async) through CaptureSpec/CapturePolicy/safe_copy plus grad save path, with reference-mode version-stamp tripwire and known-inplace auto-copy. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 8 | P2.2 (sub-milestone) | 9a6ff796 | Added `save_mode` payload retention across `CaptureSpec`, `CapturePolicy`, `safe_copy`, predicate storage, legacy `trace(save_mode=...)`, activation save paths, gradient payload copying, portable state, and fastlog disk JSON. Implemented modes `copy`, `reference`, `view`, `cpu_async`; `view` normalizes to `keep_grad=True`; reference mode emits a one-time warning, stamps tensor `_version`, raises `MutatedReferenceError` on user reads after mutation, preserves the tripwire through `record(...).to_trace()`, and falls back to copy for known in-place op outputs. Gates: ruff check . --fix pass; mypy touched source pass; focused storage/capture tests 23 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is clone_save/memory-transfer by allowing explicit no-copy/reference/view and CPU async payload retention. | Continue P2.2 hardening/docs if desired: callable per-grad `CaptureSpec(save_mode=...)` currently uses trace/static policy mode for raw grad payloads; then P2.3 arg-extraction leanness. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 8 | P2.3 (sub-milestone) | 953757b2 | Collapsed unknown-function arg extraction fallback from two BFS crawls into one tensor/parameter partition using the existing introspection expansion rules. Static/dynamic `FUNC_ARG_SPECS` paths remain unchanged; added a regression test for nested unknown args with duplicate tensors and `nn.Parameter` routing plus dynamic-cache population. Gates: ruff check . --fix pass; mypy touched source/tests pass; tests/test_arg_kwargs_extraction.py 51 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is dispatch/object-construction only for unknown functions that miss static/dynamic ArgSpecs by removing one fallback traversal. | P2.3 remaining optional scope: only widen `funcs_not_to_log` for names proven non-tensor-returning with tests; no broad additions made this round. Next round can either finish that narrow optional check or proceed to P3.1 `halt=` if P2 is accepted complete. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 9 | orchestrator note | (none) | DEFERRED-ITEMS LEDGER (do not lose): (1) P2.2 hardening — callable per-grad `CaptureSpec(save_mode=...)` not threaded into raw grad payload mode selection (trace/static policy mode used); close during P5 cleanups or declare out-of-scope with rationale. (2) P2.3 optional `funcs_not_to_log` audit — only with per-name proof + test. (3) `_postprocessing_active` reference-read carve-out (round 8) — surface in SUMMARY.md for JMT review. (4) save_mode + halt= + recurrent_layers glossary lockstep at SUMMARY stage. | Proceed to P3.1 `halt=`; P2 accepted complete by orchestrator. |
| 9 | P3.1 (record halt sub-milestone) | f1c08654 | Added `record(..., halt=...)` / `Recorder(..., halt=...)` as a separate bool predicate slot evaluated after save decisions for source, op, root-module, and child-module events. Halt-only recording is accepted; matching halt raises `HaltSignal` with the event label, marks `Recording.halted`, and preserves records through the halt boundary. Tests cover save-then-halt ordering, source-event halt, module-exit halt, invalid non-bool halt predicate failures, and existing user-raised `tl.fastlog.halt()` behavior. Gates: ruff check . --fix pass; mypy touched source/tests pass; tests/test_fastlog_halt.py 18 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is future halt rows by enabling early forward unwind from predicate mode. | Continue P3.1/P3.2: expose/finish `trace(halt=...)`, reject `halt=` with two-pass `layers_to_save`, mark partial traces with a narrow halted/frontier contract, and only then allow trace halt output-replay carve-out. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 10 | P3.1/P3.2 (trace halt sub-milestone) | d589c1c5 | Added public `trace(..., halt=...)` for PyTorch. Halt-only trace stays on the exhaustive path so saved args/validation metadata are preserved; predicate save/intervene traces keep the projected path with a trace-only finalizer. Exhaustive source/op/module-exit sites now evaluate halt after normal save/module-output decisions; halted traces expose `halted`, `halt_reason`, and `halt_frontier`, finalize a partial graph at the frontier, and validation skips only the full-model output comparison for halted traces while preserving parent replay/metadata checks. Rejects `halt=` with legacy selective two-pass `layers_to_save`/`save_grads`; MLX rejects halt clearly. Gates: ruff check . --fix pass; mypy touched source pass; tests/test_trace_halt.py + tests/test_fastlog_halt.py 22 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is future halt rows by allowing early forward unwind for full `Trace` capture. | Continue P3.3 halt-only fast path, then P3.4 cheap-hash rerun. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 11 | P3.3 (halt-only fast path) | eed49d51 | Added a narrow halt-only projected-capture fast path: when `halt` is the only active predicate surface and all save/intervention/grad defaults are off, source/op/root-module/child-module events build the live halt context, tick counters, evaluate halt, and skip save predicates, payload resolution, alias semantics, projected skipped events, backward-hook registration, and recent-history retention. Normal save/intervene captures keep save-then-halt ordering. Added halt-only recording regression coverage proving no unsaved projected events are retained. Gates: ruff touched files pass; mypy touched source/tests pass; tests/test_trace_halt.py + tests/test_fastlog_halt.py 23 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is dispatch/object-construction for halt-only rows by reducing per-event projected bookkeeping before early unwind. | Continue P3.4 cheap-hash rerun: pre-postprocess raw-event hash with normalized parent labels->order indices, divergence error on mismatch, honor original save= scope, refresh tensors in-place, rename rerun_no_save cell. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 12 | P3.4 (raw-hash/save-scope sub-milestone) | 2b58e1f9 | Added a private pre-postprocess raw-event shape hash over ordered op events, output shape/dtype, normalized parent labels->order indices, container cardinality/path, and normalized module addresses. Rerun now validates divergence against the raw hash when available, records raw hashes in rerun ledger metadata, preserves strict/non-strict divergence behavior, honors metadata-only and predicate save scopes instead of hardcoding full activation save, and renames the benchmark row to `rerun_metadata_only` with `rerun_no_save` kept as a runner alias. Gates: ruff check . --fix pass; mypy touched source pass; tests/test_intervention_phase7.py 12 pass/1 skip; targeted backward streaming regression 1 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is rerun clone_save/memory and future rerun validation by avoiding accidental full-save payload retention and establishing the raw divergence key. | Continue P3.4: implement the actual match-fast path that refreshes tensors into existing Op records and skips expensive graph rebuild/postprocess; prove every queried field repopulates or document staleness. Then P3.5 leaner differentiable replay. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 13 | P3.4 (matching-rerun fast-refresh sub-milestone) | 5e619a89 | Added a zero-divergence rerun refresh path: after raw-hash validation, rerun keeps existing graph containers when raw and final label sequences match, refreshes existing Op payload/metadata fields, refreshes aggregate Layer mirrors, updates trace run metadata, and records `fast_refresh` in last_run/state history. Divergent or label-shifted reruns fall back to the existing full state replacement. Tests prove same-shape rerun preserves Op identity while repopulating out/shape/dtype/activation_memory/transformed_out, and save_arg_values child-version snapshots refresh through the fast path. Gates: ruff check . --fix pass; mypy touched source pass; tests/test_intervention_phase7.py 14 pass/1 skip; targeted replay divergence regression 1 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is rerun object-construction/postprocess by reusing postprocessed graph containers on matching reruns. | Continue P3.4 hardening only if required: broader rerun fast-refresh coverage for modules/params/grad records or document any staleness found. Then P3.5 leaner differentiable replay. Quiet-host CPU+CUDA baseline and full benchmark gate remain deferred to end-of-sprint/P6. |
| 14 | P3.5 (differentiable replay fork sub-milestone) | 58ccbfea | Added a cone-aware private fork path for differentiable replay: affected replay-cone ops keep normal deep fork policy, while untouched ops still get distinct Op shells/source owners with shallow-copied fields to avoid deep-copying unrelated replay metadata. Differentiable replay now computes the source cone before forking and records the full-copy label set in the parent fork history. Added regression coverage proving the deep-copy label set matches the replay cone, is smaller than the full trace for a small cone, and outside-cone ops remain distinct/top-level isolated. Gates: ruff touched files pass; mypy touched source/tests pass; tests/test_intervention_phase6.py 12 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is differentiable replay object-construction/fork overhead for small cones by replacing full Op deep copies with shallow untouched-op shells. | Continue P4.1 param auto-release + lazy re-fetch, unless orchestrator wants more P3.5 profiling/hardening. Quiet-host CPU+CUDA baseline, halt rows, rerun x, and full benchmark gate remain deferred to end-of-sprint/P6. |
| 15 | P4.1 (param auto-release sub-milestone) | 89e412a2 | Released live `Param._param_ref` objects after full postprocess finalization, added lazy `Param.value`/grad re-fetch through `Trace._source_model_ref().get_parameter(address)`, and added `PostTraceParamUnavailable` for direct live-param reads after the source model is gone. Kept `_param_log_by_pid` because it stores ids/addresses only and is required for later AccumulateGrad attribution; made release idempotent for cleanup and kept aggregate grad metadata tolerant of dead-model traces during backward logging. Tests cover ref release, lazy value re-fetch, manual backward grad refresh, dead-model loud failure, and backward attribution after release. Gates: ruff check . --fix pass; mypy touched source pass; targeted P4.1/taxonomy/regression 64 pass; smoke 241 pass. Benchmark/profile numbers intentionally not collected under JMT 2026-06-12 relaxed benchmarking; expected bucket impact is memory/GC pressure by allowing model parameters to be collected after trace finalization while preserving live-model lazy access. | Continue P4.2 construction work (fields_dict / Op.__init__ scoped optimization; __slots__ remains descoped). Quiet-host CPU+CUDA baseline, halt rows, rerun x, and full benchmark gate remain deferred to end-of-sprint/P6. |
