name: Mutation Campaign

# R74 (b9 S HIGH + F MED + O LOW, round 5): the mutation driver was wired
# into no CI leg, so the zero-survivor convergence criterion had no enforced
# verdict -- margins were measured only when someone remembered to invoke it.
# TWO complementary legs now exist: weekly.yml's mutation-margin job scores
# every BOUNDED family (registry/checks/corechecks/blocks/exempt) each week
# in the canonical pinned CPU env; THIS leg automates the ~161-run per-arm
# campaign as a scheduled ROTATING shard (one of four arm shards per week --
# a full per-arm campaign is far past a single job budget), with
# workflow_dispatch available for any family. Every run archives the control
# status, per-mutant verdicts, killer node ids, and the survivor summary as
# an artifact; the job FAILS on any SURVIVOR, ERROR, or TIMEOUT (the
# driver's own exit contract -- survivors each need a new planted-corruption
# test, never an allowlist entry).

on:
  schedule:
    - cron: "41 3 * * 0"
  workflow_dispatch:
    inputs:
      family:
        description: "mutant family (registry|checks|blocks|exempt|arms|corechecks|executor|bounded=all-bounded)"
        required: false
        default: ""
      arm_shard:
        description: "arm shard I/N (only with family=arms)"
        required: false
        default: ""

permissions:
  contents: read

concurrency:
  group: mutation-campaign
  cancel-in-progress: false

jobs:
  mutation:
    name: Mutation shard
    runs-on: ubuntu-24.04
    timeout-minutes: 340
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.11"

      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86 # v5.4.2

      - name: Install Graphviz
        run: |
          sudo apt-get update
          sudo apt-get install -y graphviz

      - name: Install canonical CPU test environment
        # r7 R82 (opus MED): this leg formerly installed BARE `torch` (newest
        # resolvable) while its own header names weekly.yml's pinned job "the
        # canonical pinned CPU env". A zero-survivor gate is only meaningful
        # against a fixed interpreter: a new torch release could flip a
        # survivor either way on an unrelated upstream event. Same pins as
        # weekly.yml's mutation-margin job; pydot AND graphviz pinned
        # together (r7 R83: the DOT emitters were pinned asymmetrically here).
        # r7 R83-1: this was the repo's ONLY bare .[test] leg -- pytest
        # resolved outside the dev extra's deliberate >=8,<10 band, so a
        # pytest major could silently change what this verdict-emitting gate
        # enforces. Same extras as the weekly mutation-margin job now.
        run: |
          uv pip install --system \
            --index-url https://download.pytorch.org/whl/cpu \
            --extra-index-url https://pypi.org/simple \
            --index-strategy unsafe-best-match \
            -e ".[dev,test,tabular]" \
            "torch==2.8.0+cpu" \
            "torchvision==0.23.0+cpu" \
            "pydot==4.0.1" \
            "graphviz==0.21"

      - name: Select rotation slot
        id: slot
        env:
          INPUT_FAMILY: ${{ inputs.family }}
          INPUT_ARM_SHARD: ${{ inputs.arm_shard }}
        run: |
          # r7 R82 (opus LM): validate dispatch inputs BEFORE they reach
          # $GITHUB_OUTPUT -- a newline or shell metacharacter in a raw input
          # could forge additional step outputs through the append below.
          case "$INPUT_FAMILY" in
            ""|registry|checks|blocks|exempt|arms|corechecks|executor|bounded) ;;
            *) echo "invalid family input: $INPUT_FAMILY"; exit 1 ;;
          esac
          # Whole-string validation via case patterns ONLY (r7 R82, proven
          # bypass): grep matches PER LINE, so "1/1<newline>forged=x" passed
          # the old `grep -qE '^[0-9]+/[0-9]+$'` on its first line and the
          # embedded newline reached $GITHUB_OUTPUT as a forged output row.
          # A case pattern matches the ENTIRE string, newlines included.
          case "$INPUT_ARM_SHARD" in
            "") ;;
            *[!0-9/]*|*/*/*|/*|*/)
              echo "invalid arm_shard input: $INPUT_ARM_SHARD"; exit 1 ;;
            */*) ;;
            *) echo "invalid arm_shard input: $INPUT_ARM_SHARD"; exit 1 ;;
          esac
          if [ -n "$INPUT_FAMILY" ]; then
            family="$INPUT_FAMILY"
            shard="$INPUT_ARM_SHARD"
          else
            # 4-week rotation keyed to the ISO week: one arm shard per week
            # (the bounded families run every week in weekly.yml's
            # mutation-margin job -- single coverage authority per family).
            slot=$(( $(date -u +%V | sed 's/^0//') % 4 + 1 ))
            family="arms"
            shard="${slot}/4"
          fi
          echo "family=$family" >> "$GITHUB_OUTPUT"
          echo "shard=$shard" >> "$GITHUB_OUTPUT"
          echo "selected family=$family shard=$shard"

      - name: Run mutation campaign
        # r7 R82 (opus LM, zizmor): step outputs route through env, never
        # inline ${{ }} interpolation into the shell -- the same pattern the
        # slot step already uses for the raw dispatch inputs.
        env:
          SLOT_FAMILY: ${{ steps.slot.outputs.family }}
          SLOT_SHARD: ${{ steps.slot.outputs.shard }}
        run: |
          set -o pipefail
          mkdir -p mutation-results
          run_family () {
            local fam="$1" shard="$2" tag="$3"
            local args=(--make-sandbox "$RUNNER_TEMP/tl-mut-$tag" --family "$fam")
            if [ -n "$shard" ]; then
              args+=(--arm-shard "$shard")
            fi
            python tests/support/mutation_driver.py "${args[@]}" \
              2>&1 | tee "mutation-results/$tag.log"
          }
          status=0
          if [ "$SLOT_FAMILY" = "bounded" ]; then
            for fam in registry checks corechecks blocks exempt executor; do
              run_family "$fam" "" "$fam" || status=1
            done
          else
            run_family "$SLOT_FAMILY" "$SLOT_SHARD" "$SLOT_FAMILY" || status=1
          fi
          exit "$status"

      - name: Archive verdicts
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: mutation-verdicts-${{ github.run_id }}
          path: mutation-results/
          retention-days: 90
